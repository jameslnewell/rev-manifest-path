{
  "name": "rev-manifest-path",
  "version": "0.1.4",
  "description": "Get the cache busted path of an asset from a `gulp-rev` or `gulp-rev-all` manifest.",
  "dependencies": {
    "read-json-sync": "^1.1.0"
  },
  "devDependencies": {
    "mocha": "^2.3.4"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:jameslnewell/rev-manifest-path.git"
  },
  "scripts": {
    "test": "mocha"
  },
  "license": "MIT"
}
